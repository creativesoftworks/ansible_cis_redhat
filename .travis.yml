---

language: ruby
cache: bundler

install:
  - bundle install

before_script:
  - mkdir -p ~/.ssh
  - echo -e "$PRIVATE_SSH_KEY" > test.txt
  - cat ./test.txt
  - chmod 600 ./test.txt

script:
  - kitchen test
